PKG_CPPFLAGS='-I../inst/include/cwb'
PKG_LIBS=-L../windows/cwb/lib${R_ARCH} -lglib-2.0-0 -lpcre-1 -lcl -lws2_32

all: clean winlibs

clean:
	rm -f $(OBJECTS) $(SHLIB)

winlibs:
	"${R_HOME}/bin${R_ARCH_BIN}/Rscript.exe" --vanilla "../tools/winlibs.R"

.PHONY: all winlibs clean